// test 01c -- same violation, but the value arrives through a parameter
// expected: COMPILE OK, RUNTIME ERROR on the third call
// entry: driver
// expected output before failure: true, then false (one value per line)

routine to_bool(n : integer) : boolean is
    var b : boolean
    b := n
    return b
end

routine driver() is
    print to_bool(1)      // true
    print to_bool(0)      // false
    print to_bool(5)      // runtime error here
end
