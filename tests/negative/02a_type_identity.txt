// test 02a -- conformance of user-defined types is by name, not by structure
// expected: COMPILE ERROR

type User is record
    var id : integer
end

type Usr is record
    var id : integer
end

routine bad_assign() is
    var u : User
    var v : Usr
    u := v
end
